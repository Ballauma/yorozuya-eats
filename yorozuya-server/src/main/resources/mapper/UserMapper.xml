<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.yorozuya.mapper.UserMapper">


    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into user (openid, create_time) values (#{openid}, #{createTime})
    </insert>
    <select id="countByMap" resultType="java.lang.Integer">
        select count(*) from user
        <where>
            <if test="beginTime != null">
                and create_time &gt; #{beginTime}
            </if>
            <if test="endTime != null">
                and create_time &lt; #{endTime}
            </if>
        </where>
    </select>


    <!-- UserMapper.xml -->
    <!-- 查询指定时间范围内，每天的新增用户数量 -->
    <select id="countUserByDate" resultType="java.util.Map">
        SELECT
            DATE_FORMAT(create_time, '%Y-%m-%d') as date,
            COUNT(*) as count
        FROM user
        WHERE create_time BETWEEN #{begin} AND #{end}
        GROUP BY DATE_FORMAT(create_time, '%Y-%m-%d')
    </select>


    <!-- 查询在这个时间段之前的总用户基数 -->
    <select id="countBeforeDate" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM user WHERE create_time &lt; #{begin}
    </select>
</mapper>